
const main = () => {
  update();
  draw();
};

// TODO 1.1: Make the game loop run 5 times per second
// Hint: Check the setInterval documentation for additional arguments
// https://developer.mozilla.org/en-US/docs/Web/API/setInterval
setInterval(main);

const update = () => {

};

const draw = () => {

};
